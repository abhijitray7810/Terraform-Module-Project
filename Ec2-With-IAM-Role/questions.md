2️⃣ EC2 with IAM Role (No Access Keys)

Requirement:

Create a VPC and one public subnet

Launch an EC2 instance

Create an IAM Role with S3 read-only access

Attach IAM Role to EC2

User must NOT use access keys

Output EC2 public IP
